NEXT_PUBLIC_SUPABASE_URL=https://sqskfdcwfwywjoobbpos.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_replace_me
NEXT_PUBLIC_SITE_URL=
NEXT_PUBLIC_VERCEL_URL=

# Optional local-only fallback for UI/product smoke tests when Supabase keys are unavailable.
# Production should leave this unset or false.
NEXT_PUBLIC_STICKY_DEMO_MODE=false

# Server-only. Required for the scheduled recurrence catch-up route.
# Use a Supabase secret key (sb_secret_...) when available. Legacy service_role
# keys are supported by the code as SUPABASE_SERVICE_ROLE_KEY but should not be
# committed or exposed to the browser.
SUPABASE_SECRET_KEY=
CRON_SECRET=
